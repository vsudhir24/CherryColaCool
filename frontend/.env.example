# Get a key at https://console.cloud.google.com/google/maps-apis
VITE_GOOGLE_MAPS_API_KEY=input_ur_key

# Set to false to use the Flask API (run backend on port 8000 first)
VITE_USE_MOCK=false

# Backend base URL (used when VITE_USE_MOCK=false)
VITE_API_BASE_URL=http://localhost:8000
